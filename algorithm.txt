Ask the user for two numbers, add them, and display the sum.
Algorithm:
STEP 1: Start.
STEP 2: Ask the user to ender the first grade. Store it as grade1.
STEP 3: Ask the user to enter the second grade. Store it as grade2.
STEP 4: Ask the user to enter the third grade. Store it as grade3.
STEP 5: Compute average = (grade 1 + grade2 + grade3.)
STEP 6: Display average.
STEP 7: End.

Ask the user for a student's grade. If the grade is 60 or above, display "Passed".
Otherwise, display "Failed".
STEP 1: Start.
STEP 2: Ask the user to endter the grade. Store it as grade.
STEP 3: Check if grade is 60 or above.
STEP 4: If yes, display "Passed"
STEP 5: IF no, display"Failed"
STEP 6: END.

Ask the user for a number. If the number is 0 or above, display "Positive". Otherwise,
display "Negative".
STEP 1: Start.
STEP 2: Ask the user to enter a number. Sote it as number.
STEP 3: Check if number is greater than or equal to 0.
STEP 4: If yes, display "Positive"
STEP 5: If no, display "Negative"
STEP 6: End

Ask the user for a number. If the number is divisible by 2, display "Even". Otherwise,
display "Odd".
Step 1: Start.
Step 2: Ask the user to enter a number. Store it as number.
Step 3: Check if number MOD 2 equals 0.
Step 4: If yes, display "Even".
Step 5: If no, display "Odd".
Step 6: End.

